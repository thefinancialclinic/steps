{
  "scripts": {
    "start": "$(npm bin)/lerna run start --parallel",
    "start:prod": "(cd apps/api && node ../../.build/index.js)",
    "heroku-postbuild": "$(npm bin)/lerna run build --parallel"
  },
  "devDependencies": {},
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
