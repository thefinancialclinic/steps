{
  "scripts": {
    "start": "$(npm bin)/lerna run start --parallel",
    "start:prod": "(cd apps/api && node .build/index.js)",
    "test": "$(npm bin)/lerna run test",
    "heroku-postbuild": "$(npm bin)/lerna bootstrap && $(npm bin)/lerna run build --parallel --verbose"
  },
  "devDependencies": {},
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
